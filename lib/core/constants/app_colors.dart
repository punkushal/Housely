import 'dart:ui';

class AppColors {
  // Prevent instantiation
  AppColors._();

  // ==================== BRAND COLOR ====================

  static const Color primary = Color(0xFF7F56D9);
  static const Color primaryPressed = Color(0xFF6941C6);
  static const Color primarySoft = Color(0xFFE9D7FE);

  /* ===================== BACKGROUND ===================== */

  static const Color background = Color(0xFFFCFCFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFF2F4F7);

  /* ===================== TEXT ===================== */

  static const Color textPrimary = Color(0xFF101828);
  static const Color textSecondary = Color(0xFF475467);
  static const Color textHint = Color(0xFF9DA4AE);

  /* ===================== BORDER ===================== */

  static const Color border = Color(0xFFD2D6DB);
  static const Color divider = Color(0xFFE5E7EB);

  /* ===================== STATUS ===================== */

  static const Color success = Color(0xFF12B76A);
  static const Color successBackground = Color(0xFFD1FADF);
  static const Color error = Color(0xFFF97066);
  static const Color warning = Color(0xFFFEE4E2);

  /* ===================== OVERLAY ===================== */

  static const Color overlay = Color(0x99000000);
}
